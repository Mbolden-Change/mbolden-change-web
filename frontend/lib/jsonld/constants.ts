import {SITE_ORIGIN} from '@/lib/organizationJsonLd'

export {SITE_ORIGIN}

export const ORG_ID = `${SITE_ORIGIN}/#organization`
export const WEBSITE_ID = `${SITE_ORIGIN}/#website`

/** Default social / article image when a document has no dedicated asset. */
export const DEFAULT_SHARE_IMAGE = `${SITE_ORIGIN}/og-image.png`

export const ORG_NAME = 'mBOLDen CHANGE'
